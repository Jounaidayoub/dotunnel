{
	"name": "do",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.5.14",
		"typescript": "^5.5.2",
		"vitest": "^2.1.8",
		"wrangler": "^4.35.0"
	},
	"dependencies": {
		"hono": "^4.9.6"
	}
}
